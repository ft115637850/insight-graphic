import { SymbolResizableDirective } from './symbol-resizable.directive';

describe('SymbolResizableDirective', () => {
  it('should create an instance', () => {
    const directive = new SymbolResizableDirective();
    expect(directive).toBeTruthy();
  });
});
