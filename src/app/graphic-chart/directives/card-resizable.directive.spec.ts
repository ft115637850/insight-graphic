import { CardResizableDirective } from './card-resizable.directive';

describe('CardResizableDirective', () => {
  it('should create an instance', () => {
    const directive = new CardResizableDirective();
    expect(directive).toBeTruthy();
  });
});
