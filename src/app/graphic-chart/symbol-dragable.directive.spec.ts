import { SymbolDragableDirective } from './symbol-dragable.directive';

describe('SymbolDragableDirective', () => {
  it('should create an instance', () => {
    const directive = new SymbolDragableDirective();
    expect(directive).toBeTruthy();
  });
});
